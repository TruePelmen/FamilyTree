<Window x:Class="FamilyTree.WPF.Statistics"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:oxy="http://oxyplot.org/wpf"
        mc:Ignorable="d"
        Title="Family Tree Statistics" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Statistic Options -->
        <CheckBox x:Name="birthCheckBox" Content="Include Birth" Grid.Row="1" Margin="10"/>
        <CheckBox x:Name="deathCheckBox" Content="Include Death" Grid.Row="1" Margin="120,0,10,10"/>

        <!-- Generate Statistics Button -->
        <Button Content="Generate Statistics" Click="GenerateStatistics_Click" Grid.Row="1" Margin="240,0,10,10"/>

        <!-- OxyPlot PlotView for displaying the chart -->
        <oxy:PlotView x:Name="plotView" Grid.Row="2" Margin="10"/>
    </Grid>
</Window>